# See realtime-message-gateway/.env.example for gateway configuration

# Redis connection URL (shared by gateway and workers)
REDIS_URL=redis://localhost:6379

# Worker configuration
WORKER_ID=worker-0
